{
  "name": "stripe-backend",
  "version": "1.0.0",
  "description": "Serverless backend for Stripe payment processing",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "stripe": "^14.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
